# This file exists so the `data/` directory is checked into git.
# Put any files you want the `ls` / `cat` tools to be able to read here.
# The directory is mounted into the Ollama container at /data via
# docker-compose.yml; see README.md for usage.
